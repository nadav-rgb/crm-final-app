# הפקת דו״ח קשרים למנכ״ל

כאשר המשתמש מבקש "דו״ח", "דו״ח חדש", "הדו״ח המעודכן" או ניסוח מקביל בהקשר של פרויקט זה, יש להפיק מחדש את דו״ח הקשרים למנכ״ל באותה מתכונת, אלא אם המשתמש הגדיר במפורש דו״ח אחר.

## מקור האמת

- להפיק תמיד מנתוני Supabase החיים של `project_id = 1`, ששמו `אחדות יהודית`.
- לכלול את כל ההיסטוריה כברירת מחדל, את כל הפעילים, את כל הלקוחות כולל `is_active=false`, ואת כל רשומות הקשר כולל רשומות נגזרות.
- אין להשתמש בנתוני demo, seed, cache, קובץ קודם או מספרים שהועתקו מהפקה קודמת.
- אין לשנות נתונים ב־Supabase. ההפקה היא לקריאה בלבד.
- החישוב, המסך, Excel ו־PDF חייבים להשתמש באותו אובייקט דו״ח ובאותם totals.

## תוצרי החובה

להפיק בכל בקשה שני קבצים מעודכנים:

1. `reports/דו״ח-קשרים-אחדות-יהודית.xlsx`
2. `reports/דו״ח-קשרים-אחדות-יהודית.pdf`

בסיום יש למסור בצ׳אט שני קישורים מקומיים ישירים לקבצים, באמצעות הנתיבים המוחלטים שלהם.

## תוכן קבוע

שתי ההבהרות הבאות חייבות להופיע במפורש כנקודות הן ב־PDF והן בכל ארבעת גיליונות ה־Excel:

1. האפליקציה עדיין בפיילוט ראשוני, הערכה שמשקפת 75% מהקשרים והלקוחות האמיתיים
2. האפליקציה עלתה לאוויר לפני כחודש וחצי

הדו״ח יכלול את כל 12 המדדים לפי פעיל, שורת סיכום ארגונית, פירוט התקדמות במצוות וסיכום מצוות. אסור להשמיט פעילים או קשרים כדי לקצר את הדו״ח.

קובץ ה־Excel יכיל בדיוק את הגיליונות:

- `סיכום לפי פעיל`
- `התקדמות במצוות`
- `סיכום מצוות`
- `סיכום ארגוני`

## RTL — דרישת מסירה

- כל העברית, הכותרות והטבלאות חייבות להיות RTL תקין.
- העמודה הלוגית הראשונה, כגון `שם הפעיל`, חייבת להופיע בצד ימין של הטבלה.
- ב־PDF, `jsPDF` פועל עם `setR2L(true)`: יש להעביר שורות טבלה דרך `formatPdfRtlTableRow`, שמסדר את העמודות חזותית ומגן על סדר הספרות.
- נקודת רשימה היא תו ניטרלי. ב־PDF יש לצייר את הנקודה בנפרד בצד ימין ואת הטקסט מימינה לשמאלה לצדה, באמצעות `formatPdfRtlBullet`; אין לחבר `•` בתחילת מחרוזת עברית אחת.
- ב־Excel יש להגדיר `sheet.views[].rightToLeft = true`, ולייצב את הנקודה בעזרת סימון RTL מפורש כפי שמיושם ב־`addReportHeading`.
- אין להסתפק בבדיקת טקסט או קוד. חובה לרנדר לפחות את עמוד 1 של ה־PDF לתמונה ולבדוק חזותית: נקודות מימין, עברית קריאה, `שם הפעיל` בצד ימין, מספרים בסדר נכון וללא חיתוך.

## הפקה ואימות

מתיקיית הפרויקט יש לבצע:

```powershell
npm run verify:interaction-report
npm run generate:interaction-report
npm run build
```

אם ההפקה נכשלת ב־`fetch failed` בגלל מגבלת רשת, יש להריץ מחדש את `npm run generate:interaction-report` עם הרשאת הרשת המתאימה; אין למסור קבצים ישנים במקום הפקה חיה.

לאחר ההפקה:

- לוודא שהפקודה מדווחת `projectId: 1` ו־`projectName: אחדות יהודית`.
- לוודא ששני הקבצים קיימים ואינם ריקים, ושחתימותיהם תקינות (`PK` ל־XLSX ו־`%PDF-` ל־PDF).
- לפתוח את ה־Excel ולבדוק ארבעה גיליונות, RTL, שתי ההבהרות, מספרים כתאים מספריים ושורת סיכום.
- לרנדר ולבדוק חזותית את ה־PDF, לרבות עמוד סיכום המצוות.
- להשוות את מספר הלקוחות והקשרים בפלט ההפקה לסיכומים שבקבצים.
- להריץ `git diff --check` לפני שמירת השינוי.

## דיווח למשתמש

התשובה הסופית תהיה בעברית ותכלול בקצרה:

- אישור שהדו״ח הופק מנתונים חיים ועדכניים.
- מספר הלקוחות, הקשרים ואירועי המצוות שנמצאו בהפקה.
- אישור מפורש שבדיקת RTL חזותית בוצעה.
- קישור ישיר ל־PDF וקישור ישיר ל־Excel.

אין לטעון שהדו״ח מוכן לפני שכל בדיקות ההפקה וה־RTL הושלמו בפועל.
