node_modules/
.next/
.claude/worktrees/
.env
.env.*
!.env.example
.DS_Store
coverage/
*.log
reports/*.pdf
reports/*.xlsx

# Capacitor — android/ נשמר בגיט כדי לשמר התאמות נייטיב (אייקון/splash/הרשאות/חתימה/FCM).
# רק הסודות והתוצרים מוחרגים (ראה CAPACITOR.md).
.gradle/
android/.gradle/
android/build/
android/app/build/
android/local.properties
android/keystore.properties
*.jks
*.keystore
android/*.jks
android/*.keystore
android/key.properties
*.p12
*.pfx
*.pem
*.key
# Firebase service account (סוד שרת — לעולם לא בגיט)
*service-account*.json
serviceAccount*.json
