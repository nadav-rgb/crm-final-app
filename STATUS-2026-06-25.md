# STATUS — סגירת CRM מקרבים (ביצוע אוטונומי, 2026-06-25)

נדב, הנה מה שבוצע בזמן שהיית בחוץ, מה נדחף לפרודקשן, ומה ממתין להחלטה/בדיקה שלך.

## 🔁 עדכון סבב 2 (אחרי "תמשיך לפתח")
- ✅ **ענף `security/api-auth` מוזג ונדחף לפרוד** — אחרי smoke-test על dev server (כל 4 ה-routes מחזירים 401 בלי טוקן, לא קורסים). האבטחה חיה בפרוד.
- ✅ **F1 — עריכה+מחיקת לקוח** בפרוד. הרצתי דרך התוסף את ה-DDL: `contacts.is_active` (soft-delete) + טבלת `notifications` (שתיהן מאומתות, anon חסום). עריכת שם/טלפון/הערות + מחיקה רכה עם אישור, בפרופיל הלקוח.
- ✅ **B2 — סימון "בוצע"** בפרוד — כפתור "✓ בוצע" בתזכורות וב"פעולות היום", מנקה את המשימה ושומר ל-Supabase.
- ✅ **התראות→Supabase (cross-device) בפרוד.** במקום refactor מסוכן — dual-write+hydrate: ההתראות נכתבות ל-Supabase ומסונכרנות ל-localStorage בכניסה. אפס שינוי במסכים. **אימתתי בעצמי 6/6 מול Supabase החי** (upsert/dedup/hydrate/mark-read/anon-blocked/cleanup).
- ✅ **B1 (תשלומים) נסגר** — נדב אישר שהכללים נכונים → לא באג אלא שקיפות. נדחף: עמוד תשלומים מציג קשרים לא-מזוכים+סיבה, ופעיל רואה את הסיבה בהוספת קשר.

**נשאר רק:** B3 מיקרופון (בדיקת מכשיר עם APK) · CRON_SECRET (דשבורד Vercel).

---

## ✅ נדחף לפרודקשן (main → Vercel, אוטומטי)

1. **🔴 קריטי — דליפת RLS נסגרה.** משתמש אנונימי (בלי התחברות) קרא קודם את `contacts` ו-`interactions` (שמות+טלפונים אמיתיים) דרך ה-API הציבורי. הרצת את מיגרציית `migrations/0001_rls.sql` דרך התוסף, ואני **אימתתי מהמחשב** (`node scripts/probe-rls.mjs`) שכל הטבלאות מחזירות 0 שורות לאנונימי ו-`activist_directory` חסום. **זו הייתה החשיפה הכי חמורה — סגורה.**
2. **קומיט ה-WIP** שהיה תלוי: כפתור יציאה במובייל + push אמיתי בשיבוץ פעיל + ניקוי הוספת לקוח.
3. **תיקון `meeting-houses/completed.jsx`** — קרא רק מ-localStorage והתעלם מ-Supabase; עכשיו קורא מהענן (כמו שאר הדפים). נלווה: שמות פעילים נלקחו מנתון סטטי עם `project_id===2` שגוי → עכשיו מהרשימה החיה.
4. **ניקוי console.log אבחון** (`[CALC-DIAG]`, `[PAY-DIAG]`).
5. **UX (הערות בודקים):** קוביות הסטטיסטיקה במסך הבית עכשיו לחיצות ומנווטות (U1); כפתור בולט "✡️ עדכון התקדמות רוחנית" בפרופיל לקוח (U2 — הדף היה קיים אבל בלי שום כניסה אליו).
6. **מעטפת Capacitor OTA** — `capacitor.config.json` עם `server.url=https://crm-final-app.vercel.app/`. מעכשיו `git push` ל-main מעדכן גם את אפליקציית האנדרואיד אוטומטית.

## 📱 APK מוכן
APK חתום debug נבנה בהצלחה ונמצא על **שולחן העבודה: `crm-mekarvim.apk`** (4.4MB).
התקנה למכשיר: `adb install -r crm-mekarvim.apk`. הוא טוען את האתר החי. **לא הותקן ולא הופץ** — מחכה לך.
פרטי בנייה ושחזור ב-[CAPACITOR.md](CAPACITOR.md).

---

## ⏸️ ממתין לבדיקה שלך לפני merge (ענף `security/api-auth`)

הקשחת 4 ה-API routes שהיו ללא אימות (`reminders/schedule`, `reminders/cancel`, `push/subscribe`, `ai-summary`) — כל אחד דרש שימוש ב-service-role בלי בדיקת הרשאה (אנונימי יכל ליצור תזכורות, לרשום push לכל פעיל, ולשרוף קרדיט Anthropic).

**למה לא דחפתי לפרוד:** השינוי דורש שהקליינט ישלח `Authorization: Bearer`. חיברתי את כל הקוראים, אבל לא יכולתי לבדוק E2E מול האפליקציה החיה בלי פיקוח, ולא רציתי לנעל פיצ'ר חי בטעות. **ה-RLS כבר מכסה את החשיפה העיקרית (נתונים) — זה defense-in-depth.**

נקודה חשובה שתפסתי: `reminders/schedule` נקרא ע"י **פעיל** ששולח דוח, אז השתמשתי ב-`requireAuth` (רק "מחובר") ולא ב-`requireWriteRole` (רכז+) — אחרת זה היה שובר תזכורות לפעילים.

**מה לעשות בערב:** `git checkout security/api-auth` → התחבר כפעיל ובדוק ששליחת דוח בסיס + סיכום AI עדיין עובדים → אם כן, `git checkout main && git merge security/api-auth && git push`.

---

## 🟡 דורש החלטה שלך — B1: באג התשלומים (הכי חשוב מבין הבודקים)

**זה לא באג קוד — זו אי-התאמה בכללי-העסק.** אימתתי על נתוני אמת (`node scripts/debug-payment.cjs 11`):

```
פעיל 11, מאי 2026, אותו לקוח (4 מפגשים פרונטלי-תורני 16 דק'):
  14/5 → ₪300 ✓
  21/5 → ₪300 ✓
  28/5 → ❌ חרגת ממגבלת מפגשים עם לקוח זה
  28/5 → ❌ חרגת ממגבלת מפגשים עם לקוח זה
```

הסיבה: בקוד `PER_CONTACT_CAPS.regular = { frontal: 2, phone: 4 }` — לקוח "רגיל" משלם רק על 2 מפגשים פרונטליים בחודש. מהמפגש ה-3 ואילך — ללא תשלום. זה **בדיוק** מה שמוטי ורפאל דיווחו. הקוד עובד כפי שהוגדר; הבודקים ציפו לכללים אחרים.

**לא שיניתי כלום** — שינוי שגוי בחישובי כסף מסוכן. אני צריך ממך/מהארגון את הכללים הרשמיים:
1. מהי מגבלת המפגשים הפרונטליים בתשלום ללקוח רגיל בחודש? (כיום 2)
2. רפאל הזכיר "מפגש שבועי" ו"בונוס אחרי 3 מפגשים" — האם קיימים כללים כאלה? (כרגע אין סוג "שבועי", ויש בונוס לימוד אחרי 4 ו-6 מפגשי לימוד: ₪600/₪850)
3. אישור על המחירון: טלפוני-ידידותי ₪150, טלפוני-תורני ₪200, וידאו-תורני ₪250, פרונטלי-ידידותי ₪250, פרונטלי-תורני ₪300, רב-משתתפים ₪300.

ברגע שתאשר — התיקון הוא שינוי קבועים של דקה ב-`lib/paymentCalc.js`.

---

## 📋 ממתין (לא בוצע בכוונה — דורש DDL/החלטה/בדיקת מכשיר)

- **התראות → Supabase** (במקום localStorage). דורש טבלה חדשה (DDL שאני לא יכול להריץ לבד). הכנתי SQL מוכן — ראה למטה. אחרי שתריץ אותו, אחבר את הקוד.
- **B2 — סימון "בוצע" לתזכורת באיחור** (פער שמוטי דיווח). מוכן לביצוע, לא נגעתי כדי לא לדחוף לוגיקת-כתיבה בלי בדיקה.
- **F1 — עריכה+מחיקת לקוח.** מחיקה דורשת עמודת `is_active` (DDL).
- **B3 — מיקרופון.** ייתכן שייפתר במעטפת Capacitor (הרשאה נייטיב) — לבדוק על המכשיר עם ה-APK.
- **CRON_SECRET חלש** — החלפה דורשת עדכון גם ב-Vercel env (הדשבורד שלך).

### SQL מוכן להתראות (להריץ דרך התוסף כמו ה-RLS, כשתרצה):
```sql
create table if not exists public.notifications (
  id uuid primary key default gen_random_uuid(),
  recipient_id text not null, type text not null, title text not null,
  body text, url text, priority text default 'normal',
  read boolean not null default false, created_at timestamptz not null default now()
);
create index if not exists notifications_recipient_idx on public.notifications(recipient_id, created_at desc);
alter table public.notifications enable row level security;
drop policy if exists authenticated_all on public.notifications;
create policy authenticated_all on public.notifications for all to authenticated using (true) with check (true);
```

---

## מסמכים
- תכנון: [docs/superpowers/specs/2026-06-25-production-closure-design.md](docs/superpowers/specs/2026-06-25-production-closure-design.md)
- תכנית יישום מלאה: [docs/superpowers/plans/2026-06-25-production-closure.md](docs/superpowers/plans/2026-06-25-production-closure.md)
