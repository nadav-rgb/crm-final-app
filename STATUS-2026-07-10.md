# STATUS — ספרינט רציף CRM מקרבים (ביצוע אוטונומי, 2026-07-10)

נדב, הנה מה שבוצע בזמן שהיית בחוץ. נדחף ל-`main` (commits `5cd1c76`, `c60f0c0`) — Vercel אמור לפרוס אוטומטית.

## ✅ מה נבדק ואומת "כבר עובד" (לא נדרש שינוי קוד)
- **שינוי שם "נתי ברקוביץ'→נתי סלומון"** — כבר "נתי סלומון" גם בקוד (`AuthStore.jsx`) וגם ב-DB (`profiles.name`, activist_code 33). לא נמצא "ברקוביץ'" בשום מקום.
- **רוזי גרטש** — כבר `role=activist` ב-DB, בלי שום הרשאה מיוחדת בקוד. נבדק ישירות מול הטבלה.
- **אחדות יהודית: מקור לקוח (בית מפגש / מחוץ לבית מפגש, בונוס 250₪)** — כבר בנוי ועובד (`pages/contacts/add.jsx`).
- **נעים להכיר: לוגיקת סיור/מחוץ-לסיורים** — נשמרה כמות שהיא.
- **דיווחי הוצאות בדשבורד הפעיל** — כבר מוצגים גם ב-`my-dashboard.jsx` וגם ב-`payments.jsx`, אומת חי מול נתונים אמיתיים (בנימין קליימן — 200₪ החזר הוצאות מופיע נכון).
- **ניקוי נתוני בדיקה** — בדקתי ישירות מול ה-DB החי: 42 הלקוחות/37 הקשרים הקיימים כולם נתונים אמיתיים מ-30 הפעילים (נכנסו 2026-06-30 ואילך, אחרי הניקוי המלא שבוצע 2026-06-29). "נתן שמש" לא נמצא בשום מקום ב-DB — היה קיים רק כפעיל-דמו קבוע בקבצים `data/users.js`/`data/activists.js`, שנמחקו לגמרי (ראו למטה — היה גם מקור לבאגים אמיתיים).

## 🔧 מה תוקן בפועל

### 1. עריכת נתונים
- **עדכון מצוות** (`update-mitzvot`) — היה באג: השמירה עדכנה רק state מקומי, **לא נשמרה ל-Supabase בכלל**. אחרי רענון/מכשיר אחר הרמה חוזרת לישנה. תוקן — עכשיו נשמר.
- **עריכת קשר שדווח** — לא הייתה קיימת שום דרך לתקן/למחוק אינטראקציה קיימת. נוסף (`updateInteraction`/`deleteInteraction` + כפתורי ✏️🗑️ בהיסטוריית הקשרים בפרופיל הלקוח).
- **שיוך סיור** (נעים להכיר) — נוסף כשדה ניתן-לעריכה במודאל עריכת לקוח (בית מפגש כבר היה ניתן לעריכה קודם).

### 2. בונוס כפול
- **שורש הבאג האמיתי**: בונוס-מצוות (₪600 לעליית רמה) התבסס על state בזיכרון שהתאפס בכל רענון — **אם רכז/פעיל פתח שוב את עדכון-המצוות וחשב שהשמירה הקודמת "לא תפסה", ושמר שוב את אותה רמה, זה יצר בונוס נוסף לעליה ששולמה כבר.** תוקן: הבונוס נגזר עכשיו ישירות מ-`mitzvot_history` השמור ב-DB (אותו דפוס כמו בונוס-לקוח-חדש) — לא state שיכול "להצטבר".
- **לקוחות חדשים שהוזנו פעמיים** (למשל דרך שני פעילים, או הזנה כפולה בטעות) — נוספה בדיקת-כפילות לפי טלפון+פרויקט (`/api/contacts/check-duplicate`) שמזהירה לפני יצירת לקוח "מחוץ לבית מפגש/סיור" (המקרה שמזכה בבונוס 250₪) אם כבר קיים לקוח עם אותו טלפון. לא חוסם — רק מתריע, כי ייתכן טלפון משפחתי משותף.

### 3. יצירת לקוח דו-פרויקטלי
פעיל ששייך לשני הפרויקטים (חדוה מור יוסף, נחמיה גרטש, מוטי גלעד ועוד — 8 פעילים) מקבל עכשיו בטופס ההוספה שאלה מפורשת "לאיזה פרויקט שייך הלקוח" עם אפשרות לבחור את שניהם — במקרה כזה נוצרים **שני רישומי לקוח נפרדים**, כל אחד עם שדות המקור המתאימים לפרויקט שלו (בית מפגש / סיור).

### 4. הרשאות ובידוד נתונים — התיקון המרכזי והכי חשוב
זו הייתה החולשה החמורה ביותר שנמצאה. **עד עכשיו כל פעיל מחובר — כל role — יכול היה, דרך קונסולת הדפדפן, למשוך את כל הלקוחות/קשרים/הוצאות/דוחות של **כל** הפעילים בכל הפרויקטים**, כי:
- ה-RLS ב-Supabase (`migrations/0001_rls.sql`) הייתה מדיניות גורפת יחידה: `for all to authenticated using (true)` — כל פעולה, כל שורה, לכל מחובר.
- הקוד עצמו (`lib/CrmStore.jsx`) טען `select('*')` בלי שום סינון לפי `activist_id`/`project_id` — הבידוד היה **רק** בצד ה-React (הסתרה, לא הגנה).
- בנוסף: `pages/activists/[id].jsx` היה **בלי שום guard בכלל** — כל משתמש (כולל פעיל רגיל) שניווט ישירות ל-`/activists/17` ראה פרופיל מלא + כל הלקוחות של אותו פעיל.
- `pages/contact/[id].jsx` בדק רק "אותו פרויקט", לא "אותו פעיל" — פעיל יכול היה לצפות/לערוך/**למחוק** לקוח של פעיל אחר באותו פרויקט.
- דף הבית ("מרכז הפעילות") הציג לפעיל את "פעילות אחרונה" של **כל** הפעילים בפרויקט שלו, לא רק שלו.
- בורר הפרויקטים (בשלושת הסרגלים) הציג לכל role שאינו activist את **כל 5** הפרויקטים (כולל 2 פרויקטים בדויים), בלי בדיקת חברות בפועל — זה בדיוק מה שאיפשר לרכז פרויקט אחד לעבור ולראות נתוני פרויקט אחר. **זה גם המנגנון שהיה חושף את הדס לוי לנתוני "אחדות יהודית".**

**מה תוקן (שתי שכבות):**
1. **צד לקוח (חי, אומת, נדחף)**: כל שאילתת טעינה ב-`CrmStore.jsx` מסננת עכשיו בשרת (`.eq`/`.in`/`.overlaps`) לפי `activist_id` (פעיל) או `project_ids` (רכז/ראש-פרויקט/כספים) — לא רק תצוגה. אומתי חי מול ה-DB: פעיל רואה רק את עצמו, רכז דו-פרויקטלי (נדב) רואה בדיוק את 2 הפרויקטים הנכונים ולא את השבת/נפש הבדויים, ומעבר בין הפרויקטים מחזיר בדיוק את רשימת הפעילים הנכונה (31 מול 11). תוקנו גם: `switchProject` מאמת חברות לפני מעבר, בורר הפרויקטים בשלושת הסרגלים, guard חסר ב-`activists/[id]`, בדיקת-בעלות אמיתית (לא רק פרויקט) ב-`contact/[id]`, ו"פעילות אחרונה" בדף הבית.
2. **צד DB (RLS אמיתי) — כתוב, לא הופעל**: `migrations/0013_activist_isolation_rls.sql` מחליף את המדיניות הגורפת במדיניות אמיתית לפי `activist_id`/`project_ids` בכל הטבלאות הרגישות (contacts, interactions, expenses, base_meeting_reports, profiles, notifications, tours), כולל תיקון לפירצת **הסלמת-הרשאות** ב-`profiles` (כרגע כל משתמש מחובר יכול טכנית לעדכן את ה-`role`/`project_ids` של עצמו או של כל אחד — המיגרציה מבטלת לגמרי הרשאת כתיבה ל-`profiles` מהקליינט, כי אין באפליקציה שום מסך שכותב אליה).

   **✅ הורץ ואומת בפרודקשן (2026-07-10, מאוחר יותר באותו יום)** דרך SQL Editor. `Success. No rows returned`. אימות ב-`pg_policies`: `authenticated_all` שנותרו בטבלאות שבהיקף = **0**, מדיניות חדשות = **19**, פונקציות עזר = **3**.

   **שני באגים שנתפסו רק בזכות ההרצה בפועל** (commit `cb409d9`):
   - `current_role` היא **מילה שמורה** ב-Postgres — `create function public.current_role()` היה נכשל. שונה ל-`app_current_role()`.
   - שאילתת `pg_policies` על ה-DB החי גילתה מדיניות שלא היו מתועדות באף מיגרציה בריפו: `bmr_select/insert/update` על `base_meeting_reports`, ו-`profiles_select` על `profiles`. מדיניות PERMISSIVE מצטברות ב-**OR** — כלומר אילו הפלתי רק את `authenticated_all` והשארתי אותן, המיגרציה הייתה "מצליחה" והדליפה הייתה נשארת פתוחה **בשקט**. הקובץ מפיל עכשיו במפורש כל מדיניות קודמת בטבלאות שבהיקף.

   **אימות חי מול ה-DB** (`scripts/probe-isolation.mjs`, מתחבר כמשתמש אמיתי דרך anon key ועוקף את קוד ה-React):
   - אנונימי: 0 שורות מכל 7 הטבלאות הרגישות.
   - **פעיל רגיל (ריקי וילינגר, 37)**: רואה 4 לקוחות (רק שלה) במקום 45; 0 שורות של פעילים אחרים ב-contacts/interactions/expenses; ניסיון ממוקד למשוך את הלקוחות של פעיל 12 → **0 שורות**; ניסיון לשנות `role→ceo` → **נחסם**. לפני המיגרציה שלושתם היו מצליחים.
   - **הדס לוי (רכזת פרויקט 2)**: הוצאות = רק פרויקט 2 (שורה אחת) במקום כל 8; לקוחות פרויקט 1 = 0.
   - **נדב (רכז 1+2)**: רואה כרגיל את שני הפרויקטים שלו.
   - **כתיבה לגיטימית ממשיכה לעבוד** (הבדיקה החשובה שלא שברתי כלום): פעיל מצליח insert/update על לקוח, קשר והוצאה **שלו**; ונחסם ב-insert/update בשם פעיל אחר. שורות הבדיקה נמחקו.

### 5. באגים נוספים שנמצאו ותוקנו תוך כדי (זהות שגויה מנתוני-דמו מתים)
`data/users.js` ו-`data/activists.js` היו קבצי-דמו קבועים (מ-2026 מוקדם) עם מזהים מספריים (`id: 11`, `id: 12`...) שהתנגשו עם `activist_code` **אמיתיים** בפרודקשן (11=רפאל רייטן במציאות, אבל בקובץ הדמו 11="דוד כהן"). זה גרם לבאגים אמיתיים וחיים:
- פרופיל לקוח הראה לפעמים שם שגוי ל"פעיל אחראי".
- `base-meetings.jsx`: רכז לא ראה חלק מהדוחות של הפעילים שלו (הפרויקט של הפעיל נבדק מול הרשומה הבדויה, לא האמיתית).
- **`add-interaction/[id].jsx`: התראות "דיווח מזכה" נשלחו למנהלים עם שם פעיל שגוי** (השם הבדוי במקום השם האמיתי).
- `lib/notificationDemo.js` (`getAchdutNotificationManagers`): התראות-הסלמה ("פעיל לא מילא דו״ח עד 12:00", "דיווח מזכה נכנס לתשלומים") נשלחו למזהי-מנהלים בדויים שלא קיימים ב-Supabase בכלל — **ההתראות האלה כנראה לא הגיעו לאף אחד בפועל**. תוקן: המנהלים נטענים עכשיו מ-`activist_directory` האמיתי (עם מטמון קליל בזיכרון).
- `pages/api/reminders/schedule.js`: הרכז-ליעד-תזכורת חושב עכשיו בצד השרת (מהפרויקט של הפעיל), במקום להסתמך על רשימת-פעילים בצד לקוח — גם כדי לתקן את הבאג, וגם כי אחרי תיקון הבידוד, פעיל כבר לא טוען את רשימת כל הפעילים אז לא היה יכול למצוא את הרכז שלו בעצמו.

קבצי הדמו נמחקו לגמרי (לא נשאר עליהם שום שימוש בקוד).

### 6. יצירת לקוח — שדות
- **הוסר שדה "פעולה הבאה"** מטופס ההוספה. (לא הייתה תלות קוד אמיתית ליצירת-לקוח — `next_action` היה רק עמודה בטבלת `contacts`, לא יצר שום רשומה בטבלה אחרת. "ביטול התלות" התקיים ממילא.)
- **סרגל מצוות**: כל הערכים מאותחלים אוטומטית ל-0 כשנבחר מגדר, אינם חובה יותר, וניתן לשמור לקוח בלי לגעת בהם כלל.

### 7. באג נוסף שנמצא ותוקן: סדר Hooks ב-`contacts/add.jsx`
היה `return null` מוקדם (למשתמש שאינו activist) שממוקם **בין** קריאות `useState`/`useEffect` — מפר את כללי ה-Hooks של React ועלול לקרוס אם רכז/מנכ"ל ינווטו ישירות ל-`/contacts/add`. הועבר לסוף, אחרי כל ה-Hooks.

## קבצים ששונו
`lib/CrmStore.jsx`, `lib/AuthStore.jsx`, `lib/notificationDemo.js`, `pages/landing.jsx`, `components/DesktopLayout.jsx`, `pages/contact/[id].jsx`, `pages/contact/add-interaction/[id].jsx`, `pages/activists/[id].jsx`, `pages/base-meetings.jsx`, `pages/meeting-house-results.jsx`, `pages/contacts/add.jsx`, `pages/api/reminders/schedule.js`, `CLAUDE.md` — נערכו.
`pages/api/contacts/check-duplicate.js`, `migrations/0013_activist_isolation_rls.sql`, `scripts/probe-isolation.mjs` — נוצרו.
`data/users.js`, `data/activists.js` — נמחקו.

## הנחות עבודה שהתקבלו
1. **"רכז" בדרישה "רק רכז יראה את כלל הפעילים" פורש כ**-coord/head/ceo (מי שכבר לא activist/finance) — תואם ל-`can.seeActivists` הקיים, לא שיניתי אותו.
2. **פעילות-סיורים ("tours") נשארה ברמת-פרויקט, לא ברמת-פעיל** — סיור הוא אירוע משותף (מדריך+מארח+משתתפים), לא נתון אישי; פעיל רואה את כל הסיורים בפרויקט שלו, לא רק סיורים שהוא בהם.
3. **בדיקת-הכפילות לפני בונוס לקוח-חדש היא אזהרה (soft), לא חסימה** — כדי לא לחסום מקרה לגיטימי (טלפון משפחתי משותף).
4. **`meeting_houses`, `payment_config`, `fcm_tokens`, `push_subscriptions`, `meeting_reminders` לא נכללו במיגרציית ה-RLS** — לא נמצאו כדליפה מפורשת בבדיקה, ו-`meeting_houses` דורש בדיקה נפרדת של סמנטיקת `assigned_activists` לפני שינוי (כדי לא לשבור פיצ'ר חי בלי בדיקה מלאה). מומלץ לבדוק בספרינט הבא.
5. **לא איפסתי סיסמת אף פעיל אמיתי** כדי לבדוק את מסך "הוסף לקוח" כ-activist (מסך זה נגיש רק ל-role=activist) — לפי הזיכרון, איפוס סיסמה דורש אישור מפורש שלך בזמן-אמת, שלא היה זמין. נבדק ביסודיות בסקירת-קוד + נבדק חי כל מה שכן היה נגיש עם חשבון הרכז (נדב).
6. **מסמכי CLAUDE.md עודכנו** להסיר טענה שגויה ("localStorage בלבד, אין backend") — לא נגעתי בשאר המסמך.

## תוצאות בדיקה
- **Build**: `npm run build` — ✅ ירוק, כולל שלב ה-lint/type-check המובנה של Next ("Compiled successfully"). הורץ פעמיים (אחרי כל השינויים, ואחרי בדיקות הדפדפן) — נקי בשתיהן.
- **Lint/Test/Typecheck נפרדים**: **לא קיימים בפרויקט** — `package.json` מכיל רק `dev`/`build`/`start`, אין `.eslintrc`, אין TypeScript, אין שום framework בדיקות מותקן (jest/vitest/playwright) ואין קובץ `*.test.*` יחיד בכל הריפו. זה מצב קיים מלפני הספרינט (מתועד גם בתוכניות קודמות בריפו כ"לפרויקט אין מסגרת בדיקות אוטומטית") — לא הוספתי בדיקות חדשות כי זה מעבר להיקף המשימות שהתבקשו, אבל שווה לשקול לספרינט עתידי.
- **בדיקה חיה בדפדפן** (כ"נדב לבון", רכז דו-פרויקטלי, מול ה-DB האמיתי): התחברות ✅, בורר-פרויקטים מוגבל נכון (2 מתוך 5) ✅, רשימת פעילים מסוננת נכון per-פרויקט (31 מול 11, בדיוק החברים הנכונים) ✅, פרופיל לקוח עם שם-פעיל-אחראי נכון ✅, כפתורי עריכה/מחיקת-קשר חדשים נטענים עם הנתונים הנכונים ✅, עמוד תשלומים מחשב נכון כולל הוצאות ✅, אפס שגיאות-קונסול חדשות. **לא נבדק חי בדפדפן**: טופס "הוסף לקוח" כ-activist — במקום זה נבדקה **הכתיבה עצמה** ישירות מול ה-DB בזהות של פעיל אמיתי (ראה למטה), שזו בדיקה חזקה יותר.
- **בדיקת RLS חיה** (`scripts/probe-isolation.mjs`): אנונימי=0 מכל הטבלאות; פעיל רגיל רואה רק את שלו (4 לקוחות במקום 45) ונחסם בניסיון למשוך לקוחות של פעיל אחר ובניסיון `role→ceo`; רכזת פרויקט-2 רואה רק הוצאות פרויקט-2. **כתיבה לגיטימית ממשיכה לעבוד**: פעיל מבצע insert/update על לקוח/קשר/הוצאה שלו ✅, ונחסם בכתיבה בשם פעיל אחר ✅. שורות הבדיקה נמחקו (אומת: 0 רשומות שרד).

## האם המערכת מוכנה לעבודה?
**כן.** כל התיקונים נדחפו ל-`main` (פרוס אוטומטית ב-Vercel), ומיגרציית ה-RLS הורצה ואומתה בפרודקשן. הבידוד בין פעילים עכשיו נאכף **בשתי שכבות**: גם בשאילתות בצד השרת וגם ב-RLS של ה-DB עצמו — משתמש טכני שינסה לעקוף את הקוד דרך קריאת REST ישירה יקבל 0 שורות. פירצת הסלמת-ההרשאות ב-`profiles` (שינוי `role` של עצמך) סגורה גם היא.

נושאים לספרינט הבא (לא חוסמים):
1. **RLS ל-`meeting_houses`** — עדיין `authenticated_all`. לא נגעתי כי צריך להבין קודם את סמנטיקת `assigned_activists` כדי לא לשבור שיבוץ חי.
2. **`notifications` insert פתוח** (`with check (true)`) — מכוון: הזרימה הקיימת כותבת התראות *לפעילים אחרים* (בונוס/שיבוץ/אישור-רכז). מי שיודע activist_code יכול תיאורטית לשתול התראה לפעיל אחר. הסיכון נמוך (טקסט בפעמון), התיקון = להעביר יצירת-התראות ל-API עם service role.
3. **`tours.jsx`**: פעיל שאינו מדריך/מארח יראה "פעיל {id}" במקום שם, כי הוא כבר לא טוען את רשימת כל הפעילים. קוסמטי בלבד.
4. הוספת framework-בדיקות בסיסי (אין בפרויקט).
