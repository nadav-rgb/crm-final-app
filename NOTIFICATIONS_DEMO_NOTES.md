# התראות מערכת — דמו בלבד

נוסף מודול התראות פנימיות שמטרתו להציג לארגון את הזרימה העתידית.

מה קיים עכשיו:
- כפתור פעמון בכותרת העליונה עם מונה התראות.
- עמוד `/notifications` עם רשימת התראות.
- כפתור ניווט בסרגל: "התראות מערכת".
- התראה לפעיל כאשר הוא משובץ לבית מפגש.
- התראה למנהל/רכז כאשר נשמר שיבוץ.
- התראות דמו על דיווחי מפגשי בסיס שלא מולאו.
- התראות דמו על בית מפגש חדש שממתין לשיבוץ.

מה לא קיים עדיין בכוונה:
- אין Push אמיתי לדפדפן או למובייל.
- אין שליחת SMS או WhatsApp.
- אין backend או cron jobs.
- אין תזמון אמיתי של 23:00 / 10:00 / 11:30.

מבנה טכני:
- `/lib/notificationDemo.js` מרכז את כל לוגיקת ההתראות.
- `/pages/notifications.jsx` מציג את מרכז ההתראות.
- `DesktopLayout.jsx` מציג פעמון התראות ו-dropdown קצר.
- שיבוץ פעיל בבית מפגש יוצר התראה מקומית באמצעות localStorage.

הכנה לעתיד:
בעתיד אפשר להחליף את `notificationDemo.js` בשירות אמיתי:
- טבלת notifications ב־DB
- Scheduled jobs לתזכורות
- Web Push / Firebase Cloud Messaging
- חיבור למובייל
